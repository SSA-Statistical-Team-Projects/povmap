% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_methods.R
\name{predict.povmap}
\alias{predict.povmap}
\title{Predictions from povmap Objects}
\usage{
\method{predict}{povmap}(object, ...)
}
\arguments{
\item{object}{an object of type "povmap".}

\item{...}{additional arguments that are not used in this method.}
}
\value{
Data frame with domain predictors.
}
\description{
Method \code{predict.povmap} extracts the direct estimates, the empirical
best linear unbiased or empirical best predictors for all domains from an
povmap object.
}
\details{
For a better selection of prediction results, it is referred to use
the generic function \code{\link{estimators}}. The methods for object of
class "povmap" allows to select among the indicators of interest.
}
\examples{
\donttest{
# Example for class ebp
povmap_model <- ebp(
  fixed = eqIncome ~ gender + eqsize + cash + self_empl +
    unempl_ben + age_ben + surv_ben + sick_ben + dis_ben + rent + fam_allow +
    house_allow + cap_inv + tax_adj, pop_data = eusilcA_pop,
  pop_domains = "district", smp_data = eusilcA_smp, smp_domains = "district",
  na.rm = TRUE
)

povmap::predict(povmap_model)
}
}
\seealso{
\code{\link{direct}}, \code{\link{ebp}}, \code{\link{fh}}
}
